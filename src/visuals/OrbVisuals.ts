import Phaser from 'phaser';
import { VisualConfig, CoreId } from '../config/spellComponents';
import { getCoreTheme } from './CoreVisualTheme';
export interface OrbVisualAttachment { outerGlow: Phaser.GameObjects.Arc; mainBody: Phaser.GameObjects.Arc; innerHighlight: Phaser.GameObjects.Arc; coreSparkle: Phaser.GameObjects.Arc; update: () => void; destroy: () => void; }
export class OrbVisuals {
  static attach(scene: Phaser.Scene, sprite: Phaser.Physics.Arcade.Sprite, visual: VisualConfig, radius: number, sizeMultiplier: number, coreId: CoreId): OrbVisualAttachment { const r = radius * sizeMultiplier; const theme = getCoreTheme(coreId); const gc = theme.getGlowConfig(); const objects: Phaser.GameObjects.GameObject[] = []; const outerGlow = scene.add.circle(sprite.x, sprite.y, r + gc.radius, gc.color, gc.alpha).setDepth(7); objects.push(outerGlow); const mainBody = scene.add.circle(sprite.x, sprite.y, r, visual.color, 0.6).setDepth(8).setStrokeStyle(1.5, visual.glowColor, 0.5); objects.push(mainBody); const innerHighlight = scene.add.circle(sprite.x - r * 0.25, sprite.y - r * 0.25, r * 0.45, visual.glowColor, 0.4).setDepth(9); objects.push(innerHighlight); const coreSparkle = scene.add.circle(sprite.x, sprite.y, r * 0.2, 0xffffff, 0.5).setDepth(10); objects.push(coreSparkle); scene.tweens.add({ targets: outerGlow, scaleX: { from: 0.9, to: 1.15 }, scaleY: { from: 0.9, to: 1.15 }, alpha: { from: gc.alpha * 0.6, to: gc.alpha * 1.4 }, duration: gc.pulseSpeed, yoyo: true, repeat: -1, ease: 'Sine.easeInOut' }); scene.tweens.add({ targets: coreSparkle, alpha: { from: 0.3, to: 0.7 }, duration: 200, yoyo: true, repeat: -1 }); const ringParticles: Phaser.GameObjects.Arc[] = []; for (let i = 0; i < 3; i++) { const rd = scene.add.circle(sprite.x, sprite.y, 1.5, visual.glowColor, 0.4).setDepth(9); ringParticles.push(rd); objects.push(rd); } const tt = scene.time.addEvent({ delay: 55, loop: true, callback: () => { if (!sprite.active) return; theme.spawnTrailParticle(scene, sprite.x, sprite.y, visual, sprite.body?.velocity.x || 0, sprite.body?.velocity.y || 0); } }); const at = scene.time.addEvent({ delay: 120, loop: true, callback: () => { if (!sprite.active) return; theme.spawnAmbientParticle(scene, sprite.x, sprite.y, visual); } }); let orbitAngle = 0; const update = () => { if (!sprite.active) return; const sx = sprite.x, sy = sprite.y; outerGlow.setPosition(sx, sy); mainBody.setPosition(sx, sy); innerHighlight.setPosition(sx - r * 0.25, sy - r * 0.25); coreSparkle.setPosition(sx, sy); orbitAngle += 0.08; for (let i = 0; i < ringParticles.length; i++) { const a = orbitAngle + (i / ringParticles.length) * Math.PI * 2; ringParticles[i].setPosition(sx + Math.cos(a) * (r + 4), sy + Math.sin(a) * (r + 4)); } }; const destroy = () => { tt.destroy(); at.destroy(); scene.tweens.killTweensOf(outerGlow); scene.tweens.killTweensOf(coreSparkle); for (const obj of objects) { if (obj.active) scene.tweens.add({ targets: obj, alpha: 0, scaleX: 1.5, scaleY: 1.5, duration: 150, onComplete: () => obj.destroy() }); } theme.renderImpact(scene, sprite.x, sprite.y, visual, r * 2); }; return { outerGlow, mainBody, innerHighlight, coreSparkle, update, destroy }; }
}
